const AddPage = ({
  isAdd,
  setIsAdd,
}: {
  isAdd: boolean;
  setIsAdd: (isAdd: boolean) => void;
}) => {
  return (
    <div className="font-[Prompt] pl-0 sm:pl-15 w-full">
        Fuck you bro
    </div>
  );
};

export default AddPage;
